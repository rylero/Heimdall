# Heimdall Web UI v2: Camera Feed + Detection Overlay + Redesign

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Add raw pixel-space detection streaming from the C++ pipeline to the web UI, implement a live MJPEG camera feed with canvas detection overlay, redesign the UI with a clean monochrome navy/black/white theme, and replace the camera form with a device-discovery → activate flow.

**Architecture:** C++ pipeline adds a ZeroMQ PUB socket (port 5557) that publishes raw pixel bounding boxes before pose estimation. The web container subscribes via pyzmq in a background thread and forwards detections to the browser over a WebSocket. The browser renders MJPEG frames from an OpenCV-backed `/cameras/{id}/stream` endpoint and draws bounding boxes on a `<canvas>` overlay. Camera discovery scans `/dev/video*` on the Jetson.

**Tech Stack:** C++20 + cppzmq (existing), Python FastAPI + pyzmq + opencv-python-headless + protobuf, vanilla JS Canvas API

> **Camera feed note:** The web container opens the camera directly via OpenCV. If the C++ pipeline has already claimed the device, the stream endpoint returns 503. Use the feed for pre-deployment angle checks with the pipeline stopped.

---

## File Structure

```
proto/heimdall.proto              # Modify: add RawDetectionMsg + RawDetectionFrameMsg
src/comm/comm_layer.h             # Modify: add raw_pub_sock_ + publish_raw() + Config field
src/comm/comm_layer.cpp           # Modify: implement PUB socket + publish_raw()
src/app/heimdall_app.cpp          # Modify: call comm_.publish_raw() in on_detections
src/main.cpp                      # Modify: add raw_output_bind_addr to config
web/pyproject.toml                # Modify: add pyzmq, opencv-python-headless, protobuf, grpcio-tools dev
web/app/proto/__init__.py         # Create: empty
web/app/proto/heimdall_pb2.py     # Create: generated Python protobuf bindings
web/app/routers/cameras.py        # Modify: add /available + /{id}/stream endpoints
web/app/routers/detections.py     # Create: ZMQ subscriber thread + WebSocket /ws endpoint
web/app/main.py                   # Modify: mount detections router
web/app/static/index.html         # Replace: full UI overhaul
web/tests/test_cameras.py         # Modify: add tests for new endpoints
web/tests/test_detections.py      # Create: WebSocket + ZMQ mock tests
```

---

## Task 1: Proto + C++ Raw Detection Publisher

**Files:**
- Modify: `proto/heimdall.proto`
- Modify: `src/comm/comm_layer.h`
- Modify: `src/comm/comm_layer.cpp`
- Modify: `src/app/heimdall_app.cpp`
- Modify: `src/main.cpp`

> Builds on Jetson / in Docker. CMake re-runs protoc automatically on proto change.

- [ ] **Step 1: Add raw detection messages to proto/heimdall.proto**

Append after the last message:

```protobuf
// Raw pixel-space detection from DeepStream — published on port 5557 for debug/visualization.
// Emitted before pose estimation so bboxes are in camera pixel space (640×480).
message RawDetectionMsg {
    int32  camera_id  = 1;
    int32  class_id   = 2;
    float  confidence = 3;
    float  left       = 4;   // pixels
    float  top        = 5;
    float  width      = 6;
    float  height     = 7;
}

message RawDetectionFrameMsg {
    repeated RawDetectionMsg detections  = 1;
    uint64                   timestamp_ns = 2;
}
```

- [ ] **Step 2: Update src/comm/comm_layer.h**

```cpp
#pragma once
#include "pipeline/detection.h"
#include "pose/camera_params.h"
#include "tracker/track_event.h"
#include <optional>
#include <string>
#include <vector>
#include <zmq.hpp>

class CommLayer {
public:
    struct Config {
        std::string pose_bind_addr;       // Jetson PULL — receives robot pose
        std::string output_bind_addr;     // Jetson PUSH — sends track events
        std::string raw_output_bind_addr; // Jetson PUB  — raw pixel detections for web UI
                                          // empty string = disabled
    };

    explicit CommLayer(Config config);

    std::optional<RobotPose> try_recv_pose();

    void send_frame(const std::vector<TrackEvent>& events,
                    uint64_t timestamp_ns,
                    bool healthy = true);

    // Publish raw pixel-space detections for the web UI debug feed.
    // No-op if raw_output_bind_addr was empty at construction.
    void publish_raw(const std::vector<Detection>& detections,
                     uint64_t timestamp_ns);

    zmq::context_t& context() { return ctx_; }

private:
    zmq::context_t ctx_;
    zmq::socket_t  pull_sock_;
    zmq::socket_t  push_sock_;
    zmq::socket_t  raw_pub_sock_;
    bool           raw_enabled_ = false;
};
```

- [ ] **Step 3: Update src/comm/comm_layer.cpp**

```cpp
#include "comm_layer.h"
#include "heimdall.pb.h"

CommLayer::CommLayer(Config config)
    : ctx_(1),
      pull_sock_(ctx_, zmq::socket_type::pull),
      push_sock_(ctx_, zmq::socket_type::push),
      raw_pub_sock_(ctx_, zmq::socket_type::pub)
{
    pull_sock_.set(zmq::sockopt::rcvtimeo, 0);
    pull_sock_.bind(config.pose_bind_addr);
    push_sock_.bind(config.output_bind_addr);

    if (!config.raw_output_bind_addr.empty()) {
        raw_pub_sock_.bind(config.raw_output_bind_addr);
        raw_enabled_ = true;
    }
}

std::optional<RobotPose> CommLayer::try_recv_pose() {
    zmq::message_t msg;
    const auto result = pull_sock_.recv(msg, zmq::recv_flags::dontwait);
    if (!result) return std::nullopt;

    heimdall::RobotPoseMsg proto;
    if (!proto.ParseFromArray(msg.data(), static_cast<int>(msg.size())))
        return std::nullopt;

    return RobotPose{proto.x(), proto.y(), proto.heading()};
}

void CommLayer::send_frame(const std::vector<TrackEvent>& events,
                            uint64_t timestamp_ns,
                            bool healthy) {
    heimdall::DetectionFrameMsg frame;
    frame.set_timestamp_ns(timestamp_ns);
    frame.set_healthy(healthy);

    for (const auto& ev : events) {
        auto* msg_ev = frame.add_events();
        switch (ev.type) {
            case TrackEventType::CONFIRMED: msg_ev->set_type(heimdall::CONFIRMED); break;
            case TrackEventType::UPDATED:   msg_ev->set_type(heimdall::UPDATED);   break;
            case TrackEventType::LOST:      msg_ev->set_type(heimdall::LOST);      break;
            default: break;
        }
        auto* obj = msg_ev->mutable_object();
        obj->set_track_id(ev.object.track_id);
        obj->set_class_id(ev.object.class_id);
        obj->set_x(ev.object.x);
        obj->set_y(ev.object.y);
        obj->set_vx(ev.object.vx);
        obj->set_vy(ev.object.vy);
        obj->set_confidence(ev.object.confidence);
    }

    std::string bytes = frame.SerializeAsString();
    push_sock_.send(zmq::buffer(bytes), zmq::send_flags::none);
}

void CommLayer::publish_raw(const std::vector<Detection>& detections,
                             uint64_t timestamp_ns) {
    if (!raw_enabled_) return;

    heimdall::RawDetectionFrameMsg frame;
    frame.set_timestamp_ns(timestamp_ns);

    for (const auto& d : detections) {
        auto* raw = frame.add_detections();
        raw->set_camera_id(d.camera_id);
        raw->set_class_id(d.class_id);
        raw->set_confidence(d.confidence);
        raw->set_left(d.left);
        raw->set_top(d.top);
        raw->set_width(d.width);
        raw->set_height(d.height);
    }

    std::string bytes = frame.SerializeAsString();
    raw_pub_sock_.send(zmq::buffer(bytes), zmq::send_flags::dontwait);
}
```

- [ ] **Step 4: Update src/app/heimdall_app.cpp — publish raw before pose**

```cpp
void HeimdallApp::on_detections(const std::vector<Detection>& dets) {
    RobotPose pose;
    {
        std::lock_guard<std::mutex> lock(pose_mutex_);
        pose = latest_pose_;
    }

    const uint64_t timestamp_ns = dets.empty() ? 0ULL : dets.front().timestamp_ns;

    // Publish raw pixel detections for web UI debug feed (before pose estimation)
    comm_.publish_raw(dets, timestamp_ns);

    const auto field_dets = pose_estimator_.project(dets, pose);
    const auto events = tracker_.update(field_dets,
        static_cast<double>(timestamp_ns) * 1e-9);

    comm_.send_frame(events, timestamp_ns, /*healthy=*/true);
}
```

- [ ] **Step 5: Update src/main.cpp — add raw_output_bind_addr**

In the `HeimdallApp::Config cfg{...}` block, update the comm section:

```cpp
        .comm = {
            .pose_bind_addr       = "tcp://*:5555",
            .output_bind_addr     = "tcp://*:5556",
            .raw_output_bind_addr = "tcp://*:5557",
        },
```

- [ ] **Step 6: Build on Jetson inside Docker**

```bash
cd docker && docker compose build
```

Expected: compiles cleanly, `heimdall_proto.lib` rebuilds (new proto messages).

- [ ] **Step 7: Commit**

```bash
git add proto/heimdall.proto src/comm/comm_layer.h src/comm/comm_layer.cpp \
        src/app/heimdall_app.cpp src/main.cpp
git commit -m "feat: raw pixel detection ZMQ PUB on port 5557 for web UI overlay"
```

---

## Task 2: Web Camera Stream + Available Cameras

**Files:**
- Modify: `web/pyproject.toml`
- Modify: `web/app/routers/cameras.py`
- Modify: `web/tests/test_cameras.py`

- [ ] **Step 1: Update web/pyproject.toml**

```toml
[project]
name = "heimdall-web"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.34",
    "python-multipart>=0.0.20",
    "opencv-python-headless>=4.8",
    "pyzmq>=25",
    "protobuf>=4.21",
]

[project.optional-dependencies]
dev = ["pytest>=8", "httpx>=0.28", "grpcio-tools>=1.60"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["app"]
```

Install:

```bash
cd web && uv sync --extra dev
```

- [ ] **Step 2: Add tests for new endpoints**

Append to `web/tests/test_cameras.py`:

```python
def test_list_available_cameras_returns_list(client):
    # On Windows/dev machine, /dev/video* don't exist — should return empty list, not error
    r = client.get("/cameras/available")
    assert r.status_code == 200
    assert isinstance(r.json(), list)

def test_stream_nonexistent_camera_returns_404(client):
    r = client.get("/cameras/999/stream")
    assert r.status_code == 404

def test_stream_missing_device_returns_503(client, monkeypatch):
    # Camera exists in DB but device is unavailable
    client.post("/cameras/", json={"name": "Test", "device": "/dev/videoNONEXISTENT"})
    import cv2
    monkeypatch.setattr(cv2, "VideoCapture", lambda *a, **k: _MockCap(False))
    r = client.get("/cameras/1/stream")
    assert r.status_code == 503

class _MockCap:
    def __init__(self, ok): self._ok = ok
    def isOpened(self): return self._ok
    def set(self, *a): pass
    def read(self): return False, None
    def release(self): pass
```

- [ ] **Step 3: Run to confirm new tests fail**

```bash
cd web && uv run pytest tests/test_cameras.py -v -k "available or stream" 2>&1 | head -10
```

Expected: failures (endpoints don't exist yet).

- [ ] **Step 4: Add endpoints to web/app/routers/cameras.py**

Append after the existing `delete_camera` function:

```python
import cv2
from fastapi.responses import StreamingResponse


@router.get("/available")
def list_available_cameras():
    """Scan /dev/video0..9 for present devices. Returns empty list on non-Linux."""
    from pathlib import Path
    devices = []
    for i in range(10):
        p = Path(f"/dev/video{i}")
        if p.exists():
            devices.append({"index": i, "device": str(p), "name": f"USB Camera {i}"})
    return devices


def _mjpeg_frames(device: str):
    src = int(device) if device.isdigit() else device
    cap = cv2.VideoCapture(src)
    if not cap.isOpened():
        return  # caller checks via generator exhaustion
    cap.set(cv2.CAP_PROP_FRAME_WIDTH, 640)
    cap.set(cv2.CAP_PROP_FRAME_HEIGHT, 480)
    try:
        while True:
            ret, frame = cap.read()
            if not ret:
                break
            _, buf = cv2.imencode(".jpg", frame, [cv2.IMWRITE_JPEG_QUALITY, 70])
            yield (b"--frame\r\nContent-Type: image/jpeg\r\n\r\n"
                   + buf.tobytes() + b"\r\n")
    finally:
        cap.release()


@router.get("/{camera_id}/stream")
def stream_camera(camera_id: int):
    with get_db() as db:
        row = db.execute("SELECT device FROM cameras WHERE id = ?", (camera_id,)).fetchone()
    if not row:
        raise HTTPException(404, "Camera not found")

    device = row["device"]
    src = int(device) if str(device).isdigit() else device
    cap = cv2.VideoCapture(src)
    opened = cap.isOpened()
    cap.release()

    if not opened:
        raise HTTPException(503, "Camera device unavailable (may be in use by pipeline)")

    return StreamingResponse(
        _mjpeg_frames(device),
        media_type="multipart/x-mixed-replace; boundary=frame",
    )
```

- [ ] **Step 5: Run all camera tests**

```bash
cd web && uv run pytest tests/test_cameras.py -v
```

Expected: all 10 tests pass (7 original + 3 new).

- [ ] **Step 6: Commit**

```bash
git add web/pyproject.toml web/app/routers/cameras.py web/tests/test_cameras.py web/uv.lock
git commit -m "feat: available cameras scan and MJPEG stream endpoint"
```

---

## Task 3: Detection WebSocket

**Files:**
- Create: `web/app/proto/__init__.py`
- Create: `web/app/proto/heimdall_pb2.py` (generated)
- Create: `web/app/routers/detections.py`
- Modify: `web/app/main.py`
- Create: `web/tests/test_detections.py`

- [ ] **Step 1: Generate Python protobuf bindings**

```bash
cd web
uv run python -m grpc_tools.protoc \
    --python_out=app/proto \
    -I../proto \
    ../proto/heimdall.proto
touch app/proto/__init__.py
```

Verify: `web/app/proto/heimdall_pb2.py` now exists.

- [ ] **Step 2: Write failing tests**

Create `web/tests/test_detections.py`:

```python
import json
import threading
import time
import pytest
from fastapi.testclient import TestClient


def test_ws_connects_and_sends_no_data_without_zmq(client):
    """WebSocket connects; with no ZMQ publisher, sends nothing but stays open."""
    with client.websocket_connect("/detections/ws") as ws:
        # Should not crash or immediately close
        ws.send_text("ping")   # ignored by server
        # No data expected since ZMQ publisher not running
        # Just verify connection stays alive briefly
        time.sleep(0.1)


def test_proto_roundtrip_raw_detection():
    """RawDetectionFrameMsg serializes and deserializes correctly."""
    from app.proto import heimdall_pb2

    frame = heimdall_pb2.RawDetectionFrameMsg()
    frame.timestamp_ns = 12345
    d = frame.detections.add()
    d.camera_id = 0
    d.class_id  = 2
    d.confidence = 0.87
    d.left, d.top, d.width, d.height = 100.0, 50.0, 80.0, 60.0

    data = frame.SerializeToString()

    parsed = heimdall_pb2.RawDetectionFrameMsg()
    parsed.ParseFromString(data)
    assert parsed.timestamp_ns == 12345
    assert len(parsed.detections) == 1
    assert abs(parsed.detections[0].confidence - 0.87) < 1e-4
    assert parsed.detections[0].left == 100.0
```

- [ ] **Step 3: Run to confirm failure**

```bash
cd web && uv run pytest tests/test_detections.py -v 2>&1 | head -10
```

Expected: `ImportError` — `app.routers.detections` not found or proto import failure.

- [ ] **Step 4: Create web/app/routers/detections.py**

```python
import asyncio
import json
import os
import threading
from fastapi import APIRouter, WebSocket, WebSocketDisconnect

router = APIRouter()

# Connect to the C++ pipeline's raw detection PUB socket.
# Override via RAW_DET_ADDR env var (e.g. tcp://jetson-ip:5557).
_RAW_DET_ADDR = os.getenv("RAW_DET_ADDR", "tcp://localhost:5557")

_latest_frame: dict | None = None
_frame_lock = threading.Lock()
_subscriber_started = False
_subscriber_lock = threading.Lock()


def _start_subscriber() -> None:
    global _subscriber_started
    with _subscriber_lock:
        if _subscriber_started:
            return
        _subscriber_started = True
    t = threading.Thread(target=_zmq_loop, daemon=True, name="zmq-raw-sub")
    t.start()


def _zmq_loop() -> None:
    import zmq
    ctx = zmq.Context.instance()
    sock = ctx.socket(zmq.SUB)
    sock.connect(_RAW_DET_ADDR)
    sock.setsockopt(zmq.SUBSCRIBE, b"")
    sock.setsockopt(zmq.RCVTIMEO, 200)

    while True:
        try:
            data = sock.recv()
        except zmq.Again:
            continue
        except zmq.ZMQError:
            break
        try:
            from app.proto import heimdall_pb2
            msg = heimdall_pb2.RawDetectionFrameMsg()
            msg.ParseFromString(data)
            payload = {
                "ts": msg.timestamp_ns,
                "dets": [
                    {
                        "cam":  d.camera_id,
                        "cls":  d.class_id,
                        "conf": round(d.confidence, 3),
                        "x":    d.left,
                        "y":    d.top,
                        "w":    d.width,
                        "h":    d.height,
                    }
                    for d in msg.detections
                ],
            }
            with _frame_lock:
                global _latest_frame
                _latest_frame = payload
        except Exception:
            pass


@router.websocket("/ws")
async def ws_detections(ws: WebSocket) -> None:
    """Browser connects here to receive raw detection frames at ~30 Hz."""
    _start_subscriber()
    await ws.accept()
    try:
        while True:
            with _frame_lock:
                frame = _latest_frame
            if frame is not None:
                try:
                    await ws.send_text(json.dumps(frame))
                except Exception:
                    break
            await asyncio.sleep(0.033)   # ~30 Hz
    except WebSocketDisconnect:
        pass
```

- [ ] **Step 5: Register router in web/app/main.py**

Add after the existing router includes:

```python
from .routers import cameras, tracker, model_mgmt, thresholds, settings, detections

# ... existing includes ...
app.include_router(detections.router, prefix="/detections", tags=["detections"])
```

- [ ] **Step 6: Run tests**

```bash
cd web && uv run pytest tests/test_detections.py -v
```

Expected: both tests pass.

- [ ] **Step 7: Commit**

```bash
git add web/app/proto/ web/app/routers/detections.py web/app/main.py \
        web/tests/test_detections.py
git commit -m "feat: detection WebSocket -- ZMQ subscriber forwards raw bboxes to browser"
```

---

## Task 4: Full UI Overhaul

**Files:**
- Replace: `web/app/static/index.html`

Complete rewrite. Monochrome navy/black/white scheme. Camera matching flow. MJPEG feed toggle. Canvas detection overlay.

> No backend tests for this task — verify by running the dev server and checking each tab visually.

- [ ] **Step 1: Replace web/app/static/index.html**

```html
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Heimdall</title>
  <style>
    :root {
      --bg:       #070b14;
      --surface:  #0c1120;
      --surface2: #111827;
      --border:   #1e2d45;
      --text:     #e8edf5;
      --muted:    #6b7a96;
      --accent:   #3b82f6;
      --accent-d: #1d4ed8;
      --danger:   #ef4444;
      --success:  #22c55e;
    }
    * { box-sizing: border-box; margin: 0; padding: 0; }
    body { font-family: 'Inter', system-ui, sans-serif; background: var(--bg); color: var(--text); font-size: 14px; line-height: 1.5; }

    /* Layout */
    header { display: flex; align-items: center; gap: .75rem; padding: .9rem 1.5rem; background: var(--surface); border-bottom: 1px solid var(--border); }
    header svg { color: var(--accent); }
    header h1 { font-size: 1rem; font-weight: 600; letter-spacing: .05em; color: var(--text); }
    header span { font-size: .75rem; color: var(--muted); margin-left: auto; }

    nav { display: flex; background: var(--surface); border-bottom: 1px solid var(--border); padding: 0 1.5rem; }
    nav button { padding: .65rem 1.1rem; background: none; border: none; color: var(--muted); cursor: pointer; font-size: .875rem; font-weight: 500; border-bottom: 2px solid transparent; transition: color .15s, border-color .15s; }
    nav button:hover { color: var(--text); }
    nav button.active { color: var(--accent); border-bottom-color: var(--accent); }

    .tab { display: none; padding: 1.5rem; max-width: 960px; }
    .tab.active { display: block; }

    /* Cards */
    .card { background: var(--surface); border: 1px solid var(--border); border-radius: 8px; padding: 1.25rem; margin-bottom: 1rem; }
    .card-header { display: flex; align-items: center; justify-content: space-between; margin-bottom: 1rem; }
    .card-header h3 { font-size: .875rem; font-weight: 600; color: var(--text); text-transform: uppercase; letter-spacing: .06em; }

    /* Form fields */
    label { display: block; font-size: .75rem; font-weight: 500; color: var(--muted); margin-bottom: .3rem; text-transform: uppercase; letter-spacing: .04em; }
    input[type=text], input[type=number], select, textarea {
      width: 100%; padding: .5rem .75rem; background: var(--bg); border: 1px solid var(--border);
      color: var(--text); border-radius: 6px; font-size: .875rem; outline: none; transition: border-color .15s;
    }
    input:focus, select:focus, textarea:focus { border-color: var(--accent); }
    .field { margin-bottom: .9rem; }
    .grid2 { display: grid; grid-template-columns: 1fr 1fr; gap: .75rem; }
    .grid3 { display: grid; grid-template-columns: 1fr 1fr 1fr; gap: .75rem; }
    .grid4 { display: grid; grid-template-columns: 1fr 1fr 1fr 1fr; gap: .75rem; }
    .section-label { font-size: .7rem; font-weight: 600; color: var(--muted); text-transform: uppercase; letter-spacing: .08em; margin: 1rem 0 .6rem; border-top: 1px solid var(--border); padding-top: .75rem; }

    /* Buttons */
    .btn { display: inline-flex; align-items: center; gap: .35rem; padding: .45rem .9rem; border: none; border-radius: 6px; cursor: pointer; font-size: .8rem; font-weight: 600; transition: opacity .15s; }
    .btn:hover { opacity: .85; }
    .btn-primary   { background: var(--accent);   color: #fff; }
    .btn-secondary { background: var(--surface2); color: var(--text); border: 1px solid var(--border); }
    .btn-danger    { background: transparent; color: var(--danger); border: 1px solid var(--danger); }
    .btn-ghost     { background: none; color: var(--muted); border: 1px solid var(--border); }
    .btn-sm { padding: .3rem .65rem; font-size: .75rem; }

    /* Tags / badges */
    .badge { display: inline-block; padding: .15rem .5rem; border-radius: 4px; font-size: .7rem; font-weight: 600; }
    .badge-blue  { background: rgba(59,130,246,.15); color: #60a5fa; }
    .badge-green { background: rgba(34,197,94,.15);  color: #4ade80; }
    .badge-gray  { background: var(--surface2); color: var(--muted); }

    /* Camera cards */
    .cam-card { background: var(--surface); border: 1px solid var(--border); border-radius: 8px; margin-bottom: .75rem; overflow: hidden; }
    .cam-card-header { display: flex; align-items: center; gap: .75rem; padding: .9rem 1.1rem; cursor: pointer; }
    .cam-card-header:hover { background: var(--surface2); }
    .cam-card-header .cam-name { font-weight: 600; font-size: .9rem; }
    .cam-card-header .cam-device { color: var(--muted); font-size: .78rem; font-family: monospace; }
    .cam-card-body { padding: 1.1rem; border-top: 1px solid var(--border); display: none; }
    .cam-card-body.open { display: block; }

    /* Available camera row */
    .avail-row { display: flex; align-items: center; justify-content: space-between; padding: .6rem .75rem; background: var(--surface2); border-radius: 6px; margin-bottom: .4rem; }
    .avail-row .dev { font-family: monospace; font-size: .82rem; color: var(--text); }
    .avail-row .dev-sub { font-size: .72rem; color: var(--muted); }

    /* Feed container */
    .feed-container { position: relative; margin-top: .75rem; border-radius: 6px; overflow: hidden; background: #000; }
    .feed-container img { width: 100%; display: block; }
    .feed-container canvas { position: absolute; top: 0; left: 0; width: 100%; height: 100%; pointer-events: none; }
    .feed-placeholder { padding: 2rem; text-align: center; color: var(--muted); font-size: .82rem; }

    /* Threshold rows */
    .thresh-row { display: flex; gap: .5rem; align-items: center; margin-bottom: .4rem; }
    .thresh-row input[type=text]   { flex: 1; }
    .thresh-row input[type=number] { width: 80px; flex-shrink: 0; }

    /* Toast */
    #toast { position: fixed; bottom: 1.5rem; right: 1.5rem; padding: .6rem 1.1rem; border-radius: 6px; font-size: .82rem; font-weight: 500; display: none; z-index: 999; transition: opacity .2s; }
    #toast.ok  { background: rgba(34,197,94,.2); color: #4ade80; border: 1px solid rgba(34,197,94,.3); }
    #toast.err { background: rgba(239,68,68,.2); color: #f87171; border: 1px solid rgba(239,68,68,.3); }

    textarea { resize: vertical; font-family: monospace; font-size: .82rem; }
  </style>
</head>
<body>

<header>
  <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
    <circle cx="12" cy="12" r="3"/><path d="M12 1v4M12 19v4M4.2 4.2l2.8 2.8M17 17l2.8 2.8M1 12h4M19 12h4M4.2 19.8l2.8-2.8M17 7l2.8-2.8"/>
  </svg>
  <h1>HEIMDALL</h1>
  <span>Vision Config</span>
</header>

<nav>
  <button class="active" onclick="showTab('cameras',this)">Cameras</button>
  <button onclick="showTab('tracker',this)">Tracker</button>
  <button onclick="showTab('models',this)">Models</button>
  <button onclick="showTab('settings',this)">Settings</button>
</nav>

<!-- ── CAMERAS ──────────────────────────────────────────────── -->
<div id="tab-cameras" class="tab active">

  <div class="card">
    <div class="card-header">
      <h3>Available Devices</h3>
      <button class="btn btn-ghost btn-sm" onclick="scanCameras()">&#8635; Scan</button>
    </div>
    <div id="avail-list"><p style="color:var(--muted);font-size:.82rem">Scanning...</p></div>
  </div>

  <div class="card">
    <div class="card-header">
      <h3>Active Cameras</h3>
    </div>
    <div id="active-cam-list"><p style="color:var(--muted);font-size:.82rem">No cameras activated.</p></div>
  </div>

</div>

<!-- ── TRACKER ──────────────────────────────────────────────── -->
<div id="tab-tracker" class="tab">

  <div class="card">
    <div class="card-header"><h3>JPDA Parameters</h3></div>
    <div class="grid2">
      <div class="field"><label>Confirmation Frames</label><input type="number" id="t-confirm" min="1"></div>
      <div class="field"><label>Loss Frames</label><input type="number" id="t-loss" min="1"></div>
      <div class="field"><label>Gate Distance (m)</label><input type="number" step="0.1" id="t-gate" min="0.1"></div>
      <div class="field"><label>P(Detection)</label><input type="number" step="0.01" id="t-pd" min="0.01" max="1.0"></div>
    </div>
    <button class="btn btn-primary" onclick="saveTracker()">Save Parameters</button>
  </div>

  <div class="card">
    <div class="card-header">
      <h3>Class Thresholds</h3>
      <button class="btn btn-ghost btn-sm" onclick="addThresholdRow()">+ Add</button>
    </div>
    <div id="threshold-list"></div>
    <button class="btn btn-primary" style="margin-top:.75rem" onclick="saveThresholds()">Save Thresholds</button>
  </div>

</div>

<!-- ── MODELS ──────────────────────────────────────────────── -->
<div id="tab-models" class="tab">

  <div class="card">
    <div class="card-header"><h3>Active Model</h3></div>
    <div class="field"><label>Engine File</label><select id="active-engine-select"></select></div>
    <div class="field"><label>Num Classes</label><input type="number" id="active-num-classes" min="1" value="1" style="max-width:120px"></div>
    <div class="field"><label>Labels (one per line)</label><textarea id="active-labels" rows="7"></textarea></div>
    <button class="btn btn-primary" onclick="saveActiveModel()">Set Active</button>
  </div>

  <div class="card">
    <div class="card-header"><h3>Upload Engine</h3></div>
    <div style="display:flex;gap:.75rem;align-items:center">
      <input type="file" id="engine-upload" accept=".engine" style="flex:1">
      <button class="btn btn-primary" onclick="uploadEngine()">Upload</button>
    </div>
  </div>

  <div class="card">
    <div class="card-header"><h3>Uploaded Engines</h3></div>
    <div id="engine-list"></div>
  </div>

</div>

<!-- ── SETTINGS ─────────────────────────────────────────────── -->
<div id="tab-settings" class="tab">

  <div class="card">
    <div class="card-header"><h3>Export</h3></div>
    <p style="color:var(--muted);font-size:.82rem;margin-bottom:.9rem">Download ZIP of config database and all engine files.</p>
    <a href="/settings/export" download="heimdall_settings.zip">
      <button class="btn btn-primary">Download heimdall_settings.zip</button>
    </a>
  </div>

  <div class="card">
    <div class="card-header"><h3>Import</h3></div>
    <p style="color:var(--muted);font-size:.82rem;margin-bottom:.9rem">Upload a previously exported ZIP to restore all settings.</p>
    <div style="display:flex;gap:.75rem;align-items:center">
      <input type="file" id="import-file" accept=".zip" style="flex:1">
      <button class="btn btn-secondary" onclick="importSettings()">Import</button>
    </div>
  </div>

</div>

<div id="toast"></div>

<script>
/* ─── Utilities ─────────────────────────────────────────── */
function toast(msg, ok = true) {
  const t = document.getElementById('toast');
  t.textContent = msg;
  t.className = ok ? 'ok' : 'err';
  t.style.display = 'block';
  setTimeout(() => t.style.display = 'none', 2500);
}

function showTab(name, btn) {
  document.querySelectorAll('.tab').forEach(t => t.classList.remove('active'));
  document.querySelectorAll('nav button').forEach(b => b.classList.remove('active'));
  document.getElementById('tab-' + name).classList.add('active');
  btn.classList.add('active');
  if (name === 'cameras') { scanCameras(); loadActiveCameras(); }
  if (name === 'tracker') { loadTracker(); loadThresholds(); }
  if (name === 'models')  { loadModels(); loadActiveModel(); }
}

async function api(url, opts = {}) {
  const r = await fetch(url, {
    headers: { 'Content-Type': 'application/json', ...(opts.headers || {}) },
    ...opts,
  });
  if (r.ok) return r.json().catch(() => null);
  const e = await r.json().catch(() => ({ detail: r.statusText }));
  throw e;
}

/* ─── Detection WebSocket ───────────────────────────────── */
let detWs = null;
const cameraFeeds = {};  // cameraId -> { enabled, img, canvas, wrapper }

function initDetectionWs() {
  if (detWs && detWs.readyState < 2) return;
  try {
    detWs = new WebSocket(`ws://${location.host}/detections/ws`);
    detWs.onmessage = (e) => {
      const frame = JSON.parse(e.data);
      for (const [camId, feed] of Object.entries(cameraFeeds)) {
        if (feed.enabled && feed.canvas) drawDetections(feed.canvas, frame.dets, +camId);
      }
    };
    detWs.onclose = () => setTimeout(initDetectionWs, 3000);
  } catch (_) {}
}

function drawDetections(canvas, dets, camId) {
  const ctx = canvas.getContext('2d');
  ctx.clearRect(0, 0, 640, 480);
  ctx.lineWidth = 1.5;
  for (const d of dets) {
    if (d.cam !== camId) continue;
    ctx.strokeStyle = '#3b82f6';
    ctx.strokeRect(d.x, d.y, d.w, d.h);
    const lbl = `${d.cls} ${Math.round(d.conf * 100)}%`;
    ctx.font = '11px monospace';
    const tw = ctx.measureText(lbl).width;
    ctx.fillStyle = 'rgba(7,11,20,.85)';
    ctx.fillRect(d.x, d.y - 17, tw + 6, 15);
    ctx.fillStyle = '#93c5fd';
    ctx.fillText(lbl, d.x + 3, d.y - 5);
  }
}

/* ─── Cameras ───────────────────────────────────────────── */
let availableCams = [];
let activeCams    = [];

async function scanCameras() {
  availableCams = await api('/cameras/available');
  const el = document.getElementById('avail-list');
  if (!availableCams.length) {
    el.innerHTML = '<p style="color:var(--muted);font-size:.82rem">No cameras detected at /dev/video0-9.</p>';
    return;
  }
  el.innerHTML = availableCams.map(c => `
    <div class="avail-row">
      <div>
        <div class="dev">${c.device}</div>
        <div class="dev-sub">${c.name}</div>
      </div>
      <button class="btn btn-primary btn-sm" onclick="activateCamera('${c.device}','${c.name}')">
        Activate
      </button>
    </div>`).join('');
}

async function activateCamera(device, name) {
  try {
    await api('/cameras/', { method: 'POST', body: JSON.stringify({ name, device }) });
    toast(`${device} activated`);
    loadActiveCameras();
  } catch (e) { toast('Activate failed', false); }
}

async function loadActiveCameras() {
  activeCams = await api('/cameras/');
  const el = document.getElementById('active-cam-list');
  if (!activeCams.length) {
    el.innerHTML = '<p style="color:var(--muted);font-size:.82rem">No cameras activated.</p>';
    return;
  }
  el.innerHTML = activeCams.map(c => `
    <div class="cam-card" id="cam-card-${c.id}">
      <div class="cam-card-header" onclick="toggleCamCard(${c.id})">
        <span class="badge badge-green">&#9679;</span>
        <span class="cam-name">${c.name || 'Camera ' + c.id}</span>
        <span class="cam-device">${c.device}</span>
        <span style="margin-left:auto;color:var(--muted);font-size:1rem">&#8964;</span>
      </div>
      <div class="cam-card-body" id="cam-body-${c.id}">
        ${camFormHtml(c)}
        <div style="display:flex;gap:.5rem;margin-top:1rem;align-items:center">
          <button class="btn btn-primary btn-sm" onclick="saveCamCard(${c.id})">Save</button>
          <button class="btn btn-danger btn-sm" onclick="removeCamera(${c.id})">Remove</button>
          <label style="margin-left:auto;display:flex;align-items:center;gap:.4rem;cursor:pointer;text-transform:none;font-size:.8rem;color:var(--muted)">
            <input type="checkbox" id="feed-toggle-${c.id}"
                   onchange="toggleFeed(${c.id},this.checked)"
                   style="width:auto;accent-color:var(--accent)">
            Live Feed
          </label>
        </div>
        <div id="feed-area-${c.id}"></div>
      </div>
    </div>`).join('');

  // Restore feed state
  for (const c of activeCams) {
    if (cameraFeeds[c.id]?.enabled) {
      document.getElementById(`feed-toggle-${c.id}`).checked = true;
      renderFeed(c.id, true);
    }
  }
}

function camFormHtml(c) {
  return `
    <div class="section-label">Intrinsics</div>
    <div class="grid4">
      <div class="field"><label>fx</label><input type="number" id="cf-fx-${c.id}" value="${c.fx}"></div>
      <div class="field"><label>fy</label><input type="number" id="cf-fy-${c.id}" value="${c.fy}"></div>
      <div class="field"><label>cx</label><input type="number" id="cf-cx-${c.id}" value="${c.cx}"></div>
      <div class="field"><label>cy</label><input type="number" id="cf-cy-${c.id}" value="${c.cy}"></div>
    </div>
    <div class="section-label">Extrinsics (robot frame)</div>
    <div class="grid3">
      <div class="field"><label>X fwd (m)</label><input type="number" step="0.01" id="cf-tx-${c.id}" value="${c.tx}"></div>
      <div class="field"><label>Y left (m)</label><input type="number" step="0.01" id="cf-ty-${c.id}" value="${c.ty}"></div>
      <div class="field"><label>Z height (m)</label><input type="number" step="0.01" id="cf-tz-${c.id}" value="${c.tz}"></div>
    </div>
    <div class="grid3">
      <div class="field"><label>Yaw (°)</label><input type="number" step="1" id="cf-yaw-${c.id}" value="${c.yaw_deg}"></div>
      <div class="field"><label>Pitch down (°)</label><input type="number" step="1" id="cf-pitch-${c.id}" value="${c.pitch_deg}"></div>
      <div class="field"><label>Roll (°)</label><input type="number" step="1" id="cf-roll-${c.id}" value="${c.roll_deg}"></div>
    </div>
    <div class="section-label">Exposure</div>
    <div class="grid2">
      <div class="field"><label>Exposure (µs)</label><input type="number" id="cf-exp-${c.id}" value="${c.exposure_us}"></div>
      <div class="field"><label>Gain</label><input type="number" step="0.1" id="cf-gain-${c.id}" value="${c.gain}"></div>
    </div>`;
}

function toggleCamCard(id) {
  document.getElementById(`cam-body-${id}`).classList.toggle('open');
}

function g(id) { return +document.getElementById(id).value; }

async function saveCamCard(id) {
  const cam = activeCams.find(c => c.id === id);
  const payload = {
    name: cam.name, device: cam.device, camera_type: cam.camera_type,
    fx: g(`cf-fx-${id}`), fy: g(`cf-fy-${id}`), cx: g(`cf-cx-${id}`), cy: g(`cf-cy-${id}`),
    tx: g(`cf-tx-${id}`), ty: g(`cf-ty-${id}`), tz: g(`cf-tz-${id}`),
    yaw_deg: g(`cf-yaw-${id}`), pitch_deg: g(`cf-pitch-${id}`), roll_deg: g(`cf-roll-${id}`),
    exposure_us: g(`cf-exp-${id}`), gain: g(`cf-gain-${id}`),
  };
  try {
    await api(`/cameras/${id}`, { method: 'PUT', body: JSON.stringify(payload) });
    toast('Camera saved');
  } catch (e) { toast('Save failed', false); }
}

async function removeCamera(id) {
  if (!confirm('Remove this camera?')) return;
  try {
    const r = await fetch(`/cameras/${id}`, { method: 'DELETE' });
    if (!r.ok) throw new Error();
    delete cameraFeeds[id];
    loadActiveCameras();
    toast('Camera removed');
  } catch (e) { toast('Remove failed', false); }
}

function toggleFeed(camId, enabled) {
  if (!cameraFeeds[camId]) cameraFeeds[camId] = { enabled: false };
  cameraFeeds[camId].enabled = enabled;
  renderFeed(camId, enabled);
  if (enabled) initDetectionWs();
}

function renderFeed(camId, enabled) {
  const area = document.getElementById(`feed-area-${camId}`);
  if (!area) return;
  if (!enabled) {
    area.innerHTML = '';
    if (cameraFeeds[camId]) cameraFeeds[camId].canvas = null;
    return;
  }
  const wrapper = document.createElement('div');
  wrapper.className = 'feed-container';

  const img = document.createElement('img');
  img.src = `/cameras/${camId}/stream`;
  img.onerror = () => {
    wrapper.innerHTML = '<div class="feed-placeholder">Camera unavailable — may be in use by pipeline, or no device at this path.</div>';
  };

  const canvas = document.createElement('canvas');
  canvas.width = 640; canvas.height = 480;

  wrapper.appendChild(img);
  wrapper.appendChild(canvas);
  area.innerHTML = '';
  area.appendChild(wrapper);

  if (!cameraFeeds[camId]) cameraFeeds[camId] = {};
  cameraFeeds[camId].canvas = canvas;
}

/* ─── Tracker ───────────────────────────────────────────── */
async function loadTracker() {
  const d = await api('/tracker/');
  document.getElementById('t-confirm').value = d.confirmation_frames;
  document.getElementById('t-loss').value    = d.loss_frames;
  document.getElementById('t-gate').value    = d.gate_distance;
  document.getElementById('t-pd').value      = d.p_detection;
}

async function saveTracker() {
  try {
    await api('/tracker/', { method: 'PUT', body: JSON.stringify({
      confirmation_frames: +document.getElementById('t-confirm').value,
      loss_frames:         +document.getElementById('t-loss').value,
      gate_distance:       +document.getElementById('t-gate').value,
      clutter_density: 1.0,
      p_detection:         +document.getElementById('t-pd').value,
    })});
    toast('Parameters saved');
  } catch (e) { toast('Save failed', false); }
}

/* ─── Thresholds ────────────────────────────────────────── */
let threshData = [];

async function loadThresholds() {
  threshData = await api('/thresholds/');
  renderThresholds();
}

function renderThresholds() {
  document.getElementById('threshold-list').innerHTML = threshData.map((t, i) => `
    <div class="thresh-row">
      <input type="number" value="${t.class_id}" min="0" style="width:64px;flex-shrink:0"
             oninput="threshData[${i}].class_id=+this.value">
      <input type="text" value="${t.class_name}" placeholder="class name"
             oninput="threshData[${i}].class_name=this.value">
      <input type="number" value="${t.threshold}" step="0.01" min="0" max="1"
             oninput="threshData[${i}].threshold=+this.value">
      <button class="btn btn-danger btn-sm" onclick="threshData.splice(${i},1);renderThresholds()">&#10005;</button>
    </div>`).join('');
}

function addThresholdRow() {
  const next = threshData.length ? Math.max(...threshData.map(t => t.class_id)) + 1 : 0;
  threshData.push({ class_id: next, class_name: '', threshold: 0.3 });
  renderThresholds();
}

async function saveThresholds() {
  try {
    await api('/thresholds/', { method: 'PUT', body: JSON.stringify(threshData) });
    toast('Thresholds saved');
  } catch (e) { toast('Save failed', false); }
}

/* ─── Models ────────────────────────────────────────────── */
async function loadModels() {
  const engines = await api('/models/');
  const sel = document.getElementById('active-engine-select');
  sel.innerHTML = '<option value="">— none —</option>' +
    engines.map(e => `<option value="${e}">${e}</option>`).join('');
  document.getElementById('engine-list').innerHTML = engines.length
    ? engines.map(e => `
        <div style="display:flex;justify-content:space-between;align-items:center;padding:.5rem 0;border-bottom:1px solid var(--border)">
          <span style="font-family:monospace;font-size:.82rem">${e}</span>
          <button class="btn btn-danger btn-sm" onclick="deleteEngine('${e}')">Delete</button>
        </div>`).join('')
    : '<p style="color:var(--muted);font-size:.82rem">No engines uploaded.</p>';
}

async function loadActiveModel() {
  const m = await api('/models/active');
  document.getElementById('active-engine-select').value = m.engine_filename || '';
  document.getElementById('active-num-classes').value = m.num_classes;
  document.getElementById('active-labels').value = (m.labels ?? []).join('\n');
}

async function saveActiveModel() {
  const fn = document.getElementById('active-engine-select').value || null;
  const labels = document.getElementById('active-labels').value
    .split('\n').map(s => s.trim()).filter(Boolean);
  try {
    await api('/models/active', { method: 'PUT', body: JSON.stringify({
      engine_filename: fn,
      num_classes: +document.getElementById('active-num-classes').value,
      labels,
    })});
    toast('Active model saved');
  } catch (e) { toast('Save failed: ' + JSON.stringify(e), false); }
}

async function uploadEngine() {
  const file = document.getElementById('engine-upload').files[0];
  if (!file) { toast('Select a .engine file first', false); return; }
  const fd = new FormData(); fd.append('file', file);
  try {
    const r = await fetch('/models/upload', { method: 'POST', body: fd });
    if (!r.ok) throw await r.json();
    toast('Uploaded ' + file.name); loadModels();
  } catch (e) { toast('Upload failed', false); }
}

async function deleteEngine(name) {
  if (!confirm('Delete ' + name + '?')) return;
  try {
    const r = await fetch('/models/' + name, { method: 'DELETE' });
    if (!r.ok) throw new Error();
    loadModels(); loadActiveModel(); toast('Deleted ' + name);
  } catch (e) { toast('Delete failed', false); }
}

/* ─── Settings ──────────────────────────────────────────── */
async function importSettings() {
  const file = document.getElementById('import-file').files[0];
  if (!file) { toast('Select a .zip file first', false); return; }
  const fd = new FormData(); fd.append('file', file);
  try {
    const r = await fetch('/settings/import', { method: 'POST', body: fd });
    if (!r.ok) throw await r.json();
    toast('Settings imported — reload page to see changes');
  } catch (e) { toast('Import failed', false); }
}

/* ─── Init ──────────────────────────────────────────────── */
scanCameras();
loadActiveCameras();
</script>
</body>
</html>
```

- [ ] **Step 2: Start dev server and verify**

```bash
cd web && uv run uvicorn app.main:app --port 8080 --reload
```

Open `http://localhost:8080`. Check:
- [ ] Header shows "HEIMDALL" with scan icon
- [ ] All 4 nav tabs switch cleanly
- [ ] Cameras tab: "Available Devices" card with Scan button, "Active Cameras" card
- [ ] Tracker tab: clean 2-column grid, no clutter
- [ ] Models tab: engine select, labels textarea, upload section
- [ ] Settings tab: export link, import input
- [ ] Toast appears on any save action

- [ ] **Step 3: Run full test suite**

```bash
cd web && uv run pytest tests/ -q
```

Expected: 29+ tests pass (previous 27 + 3 new cameras tests + 2 detections tests).

- [ ] **Step 4: Commit**

```bash
git add web/app/static/index.html
git commit -m "feat: monochrome UI redesign with camera matching and detection overlay"
```

---

## Milestone Complete — Criteria

| Check | Condition |
|-------|-----------|
| C++ build | `docker compose build` succeeds, new proto messages compile |
| Raw detection pub | `publish_raw()` called in HeimdallApp::on_detections |
| Web tests | `uv run pytest tests/` — 29+ passed |
| Camera scan | `/cameras/available` returns `/dev/video*` list on Jetson |
| MJPEG stream | `/cameras/{id}/stream` delivers frames when device free |
| WebSocket | `/detections/ws` connects, forwards ZMQ frames at ~30 Hz |
| UI | Clean monochrome theme, camera card expand/collapse, feed toggle, bbox canvas overlay |
